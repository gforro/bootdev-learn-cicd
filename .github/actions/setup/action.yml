name: 'Setup project'
description: 'Checks out project and sets up go'
runs:
  using: 'composite'
  steps:
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: "1.23.0"